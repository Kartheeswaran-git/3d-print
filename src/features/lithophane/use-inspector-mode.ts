/** Simple / Advanced inspector mode, shared with the keychain studio (one module, one key). */
export { INSPECTOR_MODE_STORAGE_KEY, setInspectorMode, useInspectorMode } from "@/lib/state/inspector-mode";
