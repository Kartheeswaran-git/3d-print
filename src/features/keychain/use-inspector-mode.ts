/** Simple / Advanced inspector mode, shared with the moon lamp studio (one module, one key). */
export {
  DEFAULT_INSPECTOR_MODE,
  INSPECTOR_MODE_STORAGE_KEY,
  parseInspectorMode,
  setInspectorMode,
  useInspectorMode,
} from "@/lib/state/inspector-mode";
